import { styles } from '../../../../config'

const {
    colors,
    spaces,
} = styles

export default {
    select: {
        width: '100%',
        padding: [spaces[1], spaces[2]],
        borderBottom: `2px solid ${colors.keyline}`,
    },
}
